# 2026-07-17 edu_rag 系统状态快照

## 状态保存（16:40）
用户要求保存系统当前状态，无新代码变更。以下为截至 2026-07-17 的完整状态快照。

## 项目整体进度

### 已完成的工作（2026-07-16）

1. **代码库评估**：全面审查架构、安全、冗余、性能四维度，输出 `项目代码库评估报告.md`
   - 整体评分：架构 4.8/5 · 安全 3.2→4.5/5 · 代码质量 4.0→4.5/5

2. **前端全面重构**：设计系统升级 + 布局重构 + 考试出题/答题模块重点优化 + 微交互动画 + 响应式适配
   - 涉及文件：vars.css、global.css、App.vue、ExamPage.vue、QAPage.vue、HomePage.vue、KnowledgePage.vue、UploadPage.vue、PageHeader.vue、EmptyState.vue
   - 验证：vue-tsc + vite build 零错误通过

3. **后端安全修复（P1）**：6/7 项完成
   - P1-1 鉴权 fail-closed ✅
   - P1-2 前端 XSS DOMPurify ✅
   - P1-3 docker-compose 强制密码 ✅
   - P1-5 分页参数 le=100 ✅
   - P1-6 API Key 仅 Header ✅
   - P1-7 body 限制统一 ✅
   - P1-4 依赖锁文件 ⬜ 待办

4. **后端性能优化（P2）**：10/10 项全部完成
   - P2-1 删死代码 🟡 部分（仅删未用 import，其余有测试覆盖保留）
   - P2-2 抽取重复逻辑 ✅（json_utils + paginated_select + _prepare_qa_context + _prepare_exam_context + _build_sources 合并）
   - P2-3 redis SCAN ✅
   - P2-4 上传分块写盘 ✅
   - P2-5 asyncio.to_thread ✅
   - P2-6 BM25 延迟重建 ✅
   - P2-7 compressor 并发 ✅
   - P2-8 路径遍历校验 ✅
   - P2-9 魔数校验 ✅
   - P2-10 CORS 收紧 ✅
   - 额外：retrieval_logger 异步写 ✅、session 增量更新 ✅、milvus 延迟 flush ✅、ingress 盲删优化 ✅、cache L1 LRU ✅

### 待办事项

| 优先级 | 编号 | 内容 | 说明 |
|---|---|---|---|
| P1 | P1-4 | 依赖锁文件 | 需生成 uv.lock / requirements.txt |
| P3 | P3-1 | JWT + RBAC 用户体系 | 替代单一共享 API Key |
| P3 | P3-2 | 补齐测试覆盖 | parsers/cleaners/middleware/session/config/ORM |
| P3 | P3-3 | 统一命名与文档 | ingress/Ingestion 对齐；接口计数统一为 13 |
| P3 | P3-4 | 清理 LLM 客户端双入口 | 迁移引用并删除 generation/llm/ shim |
| P3 | P3-5 | 配置集中化 | 限流参数/SESSION_TTL/batch_size/向量维度进配置 |
| P3 | P3-6 | 异常处理收窄 | 修复吞异常、过宽 except、嵌套 try-except |
| P3 | P3-7 | 全局可变状态加 LRU | _bm25_indexes（跳过）、_l1（已完成） |
| P3 | P3-8 | 落实架构演进建议 | Post-Retrieval 阶段归属、Context Augmentation Pipeline |
| P3 | P3-9 | 会话历史字段结构化 | schemas.py 改 list[ChatMessage]/list[AnswerItem] |

### 新增依赖
- `aiofiles==25.1.0`（后端异步文件 IO）
- `dompurify`（前端 XSS 净化）

### 新增文件
- `src/shared/json_utils.py`（鲁棒 JSON 解析工具）
- `项目代码库评估报告.md`（评估报告）
- `.workbuddy/memory/` 记忆体系（MEMORY.md + 2026-07-16.md + 2026-07-17.md）

## 关键架构路径（供后续引用）
- 接口契约：`src/interfaces/__init__.py`（13 个 ABC）
- Retrieval 门面：`src/retrieval/service.py`
- QA 引擎：`src/generation/qa_engine.py`（P2-2 重构后含 _prepare_qa_context）
- Exam 引擎：`src/generation/exam_engine.py`（P2-2 重构后含 _prepare_exam_context）
- FastAPI 入口：`src/orchestration/app.py`
- 鉴权中间件：`src/orchestration/middleware/auth.py`（P1-1 fail-closed）
- 通用分页：`src/orchestration/pagination.py`（P2-2 新增 paginated_select）
- JSON 工具：`src/shared/json_utils.py`（P2-2 新增）
- 配置：`src/shared/config.py`（SecretStr 脱敏）

## P0 并发容灾前置改造（16:50-17:25）

基于批准方案（`~/.workbuddy/plans/electric-vortex-darwin.md`）完成 P0 五项 + P1-4：
- C1 Milvus docker 服务 + pymilvus 依赖 + requirements.txt 锁文件（384 行）
- C2 chroma→milvus 迁移脚本（scripts/migrate_chroma_to_milvus.py）
- C3 BM25 持久化（orm.py BM25IndexCache 表 + keyword persist/load + app lifespan 加载）
- C4 uvicorn --workers 4
- C8 /health 探活依赖（MySQL+Redis+向量库，503 降级）

## P1/P2 并发容灾改造（17:25-18:00）

完成 P1（6项）+ P2（5项）全部 11 项：
- C5 ARQ 异步任务队列（worker 模块 4 文件 + documents.py 投递 + docker worker 服务）
- C7 MySQL 连接池调优（pool_size 30 + max_overflow 50 + pool_pre_ping）
- D2 文件存储抽象（src/shared/storage.py 本地/对象存储 + StorageConfig）
- D4 云部署（docker-compose.cloud.yml）
- D5 MySQL 主从（master.cnf/slave.cnf）
- D8 failover 脚本（scripts/failover_check.py）
- C9 压测（tests/load/locustfile + mock_llm_server + README）
- C10 Prometheus 业务指标（middleware/metrics.py + app.py 接入）
- D6 Redis 哨兵（docker/redis/sentinel.conf）
- D7 Milvus 集群说明（docker/milvus/cluster.md）
- D10 容灾手册（docs/disaster-recovery.md）

### 审查发现（docs/P1P2改造审查报告.md）
- P1-1 已修复：ARQ worker 未初始化连接 → 加 on_startup/on_shutdown
- P2×8：BM25 竞争、/health 开销、指标未埋点、删除双重删、failover 无冷却、Dockerfile uvicorn/锁文件未用、worker 重试文件清理
- P3×4：pool 未关、remove 残留、维度硬编码、Dockerfile 双份维护

### 关键新增文件
- src/orchestration/worker/{__init__,tasks,client,config}.py
- src/shared/storage.py
- src/orchestration/middleware/metrics.py
- scripts/{migrate_chroma_to_milvus,failover_check}.py
- docker/{docker-compose.cloud.yml,mysql/{master,slave}.cnf,redis/sentinel.conf,milvus/cluster.md}
- tests/load/{locustfile,mock_llm_server}.py + README.md
- docs/{disaster-recovery.md,P1P2改造审查报告.md}

## 并发容灾 P0 改造（16:50-进行中）

基于用户批准的方案（`C:\Users\lenovo\.workbuddy\plans\electric-vortex-darwin.md`），推进 P0 前置改造。
目标：①压测 100 万总请求（峰值几千 QPS）②本地崩溃分钟级 RTO 切换云。

### 已完成
- **C1（部分）**：`pyproject.toml` 加 `pymilvus>=2.4.0`；`Dockerfile` 补 pymilvus/rank-bm25 + `--workers 4`；`docker-compose.yml` 加 Milvus 三件套（etcd+minio+milvus）+ provider 切 milvus
- **C2**：新增 `scripts/migrate_chroma_to_milvus.py`（幂等迁移，维度校验，dry-run 支持）
- **C3**：BM25 持久化 — `orm.py` 加 `BM25IndexCache` 表；`bm25.py` 加 `to_state/from_state`；`keyword/__init__.py` 加 `persist_bm25_to_db`/`load_all_bm25_from_db` + 写操作 fire-and-forget 落盘；`app.py` lifespan 启动加载
- **C4**：`Dockerfile` CMD 加 `--workers 4`（多 worker，lifespan 已含预热）
- **C8**：`app.py /health` 改探活 MySQL+Redis+向量库，任一失败返回 503（容灾 LB 探活前提）

### 进行中
- **C1 锁文件**：`requirements.txt` 生成（pip-compile，网络不稳多次重试，现用腾讯镜像无哈希版）

### 关键发现
- Milvus 代码层早已就绪（`milvus.py` 181 行生产级 + 工厂模式 + 配置开关），C1 主要是部署+依赖+迁移，非重写
- 多 worker 下 BM25 写竞争需 C5（异步任务队列）配合：文档解析走单 worker 队列即可天然解决
- 当前单实例并发上限约 10-20（MySQL pool_size=10 + LLM 限速），距峰值几千 QPS 差 2-3 个数量级

## 使用文档生成（23:43-00:12）

生成 `docs/启动与使用文档.md`，覆盖：
- Docker 启动完整流程（docker compose 后端 + npm run dev 前端 + 停止/重建）
- PyCharm 启动完整流程（.env 配置 + Conda EduRAG + app/worker 运行配置）
- API 鉴权（X-API-Key header + Swagger Authorize + 前端）
- 数据库查看（MySQL docker exec/GUI 3307 + Redis docker exec/GUI 6380 + Milvus Attu 8001/python）
- 常见操作 + 6 个 FAQ

关键凭证：API Key=dev_test_key_2026 · Redis=redis_dev_2026 · MySQL 密码见 .env MYSQL__PASSWORD
