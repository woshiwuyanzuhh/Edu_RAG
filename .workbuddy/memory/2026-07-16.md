# 2026-07-16 edu_rag 项目代码库评估

## 完成事项
- 完成 edu_rag 项目全面代码审查，覆盖架构、安全、冗余、性能四个维度
- 输出报告：`项目代码库评估报告.md`（项目根目录）

## 关键发现
- **架构评分 4.8/5**：四层 RAG 分层清晰，13 个 ABC 接口契约，六边形架构落地到位
- **安全评分 3.2/5**：3 个严重漏洞
  - C1: `.env:7` 真实 DeepSeek API Key 明文（sk-8d15...）需立即轮换
  - C2: `.env:29` MySQL 弱口令 `123456`
  - C3: `auth.py:36-38` APP__API_KEY 为空时全部端点无鉴权放行（fail-open）
- **高危**：`QAPage.vue:243,378` marked + v-html 存储型 XSS（无 DOMPurify）
- **代码质量 4.0/5**：约 200 行死代码（generation/llm shim、8 个未用 Response schema、Chunk 类等）；qa_engine/exam_engine 流式与非流式主体重复
- **性能瓶颈**：`cache.py:155` redis.keys 阻塞、`documents.py:51` 文件全量入内存、`bm25.py` 增删全量重建、`compressor.py` 串行 LLM 压缩

## 改进优先级
- P0: 轮换 API Key、fail-closed 鉴权、强密码
- P1: DOMPurify、docker-compose 弱密码、依赖锁文件、分页上限
- P2: 删死代码、抽重复逻辑、redis SCAN、分块写盘
- P3: JWT+RBAC、补测试、统一命名文档

## 状态更新（17:00）
- 用户确认：P0 的 API Key（`.env:7`）和 MySQL 口令（`.env:29`）由用户自行处理，后续不再追踪
- 其余所有问题已持久化到 `MEMORY.md` 待办清单（P1-1 ~ P1-7、P2-1 ~ P2-10、P3-1 ~ P3-9）
- 等待用户指定下一步执行哪一项
- 关键待办：
  - P1-1 鉴权 fail-closed（`auth.py:36-38`）—— 用户未排除，仍需处理
  - P1-2 前端 XSS DOMPurify（`QAPage.vue:243,378`）
  - P2-3 redis.keys → SCAN（`cache.py:155`）生产必改
  - P2-1 删除约 200 行死代码（`generation/llm/` shim 等）

## 前端全面重构（17:05-17:25）
- 用户要求重构整个前端，重点优化布局、考试出题与答题模块、视觉统一、动画、响应式
- **完成内容**：
  1. 升级设计系统 `vars.css`：新增字体体系（font-family/字号阶梯/字重/行高）、响应式断点、高级过渡曲线（ease-premium/ease-spring）、按钮状态 token、z-index 层级、品牌渐变
  2. 升级 `global.css`：统一按钮样式（主/次/危险按钮磁吸 hover+按压反馈）、统一卡片样式、动画库（fade-in/scale-in/slide-in/stagger-in）、页面切换过渡、响应式工具类
  3. 重构 `App.vue`：移除硬编码 #001529、移动端汉堡菜单+抽屉导航、页面切换过渡动画、品牌渐变文字、主题切换旋转动画
  4. 重构 `ExamPage.vue`（核心）：出题配置（radio 按钮组+滑块标记+难度色彩）、答题区（可点击选项卡片+判断题双按钮+简答文本域）、批改结果（环形进度+雷达图+图例+逐题反馈卡片正确/错误状态）、考试状态条（计时+答题进度）、交错入场动画、响应式栅格
  5. 重构 `QAPage.vue`：桌面固定侧边栏+移动端抽屉、AI 头像渐变、消息气泡阴影、参考来源区块、操作按钮 hover 显现、输入区圆角增强
  6. 优化 `HomePage.vue`：Hero badge scale-in、统计卡片图标背景色块+stagger 入场、功能卡片图标缩放旋转 hover、步骤序号渐变
  7. 优化 `KnowledgePage.vue`/`UploadPage.vue`：统一卡片入场动画、上传区 hover 增强
  8. 优化 `PageHeader.vue`/`EmptyState.vue`：图标背景色块、slide-in 入场、hover 缩放
- **验证**：`npm run build` 通过（vue-tsc 类型检查 + vite build 零错误），21.95s 完成
- dev server 运行于 http://localhost:5173/

## 后端安全与性能修复（17:41）
按 P1/P2 优先级批量修复，后端编译检查 + 前端构建均零错误通过：

| 编号 | 修复内容 | 文件 |
|---|---|---|
| P1-1 | 鉴权改 fail-closed：生产模式未配置 API Key 时返回 503 拒绝访问 | `src/orchestration/middleware/auth.py` |
| P1-6 | 移除 Query 传 API Key，仅允许 X-API-Key 请求头 | `src/orchestration/middleware/auth.py` |
| P2-3 | redis.keys(pattern) 改 scan_iter 迭代，避免生产阻塞 | `src/shared/cache.py` invalidate 方法 |
| P1-7 | body 限制从硬编码 10MB 改为读取 settings.app.max_upload_size_mb | `src/orchestration/app.py:41` |
| P1-5 | 4 个列表端点分页参数加 Query(ge=1, le=100) 上限 | knowledge/documents/qa/exam API |
| P1-3 | docker-compose 移除默认弱密码，MySQL/Redis 强制要求环境变量 | `docker/docker-compose.yml` |
| P1-2 | 前端接入 DOMPurify 净化 marked 输出，堵住存储型 XSS | `frontend/src/views/QAPage.vue` + npm install dompurify |
| P2-1 | 删除 app.py 未用 Request import（其余死代码有测试覆盖，保留） | `src/orchestration/app.py:24` |

验证：
- 后端 `py_compile` 7 个修改文件全部通过
- 前端 `vue-tsc + vite build` 零错误通过（18.80s），DOMPurify 集成正确

未完成（留待后续）：
- P1-4 依赖锁文件（需生成 uv.lock / requirements.txt）
- P2-2 抽取重复逻辑（_prepare_qa_context 等）
- P2-4~P2-10 其他性能优化
- P2-1 剩余死代码（generation/llm shim 等有测试覆盖，需专门重构阶段处理）

## 性能优化批量修复（18:00）
聚焦 P2 性能瓶颈，13 个后端文件编译检查全部通过：

| 编号 | 优化内容 | 文件 | 优化前 → 优化后 |
|---|---|---|---|
| P2-4 | 上传文件分块写盘 | `documents.py` | 全量 read 入内存 → aiofiles 1MB 分块写盘 |
| P2-9 | 文件魔数校验 | `documents.py` | 仅扩展名校验 → 纯 Python 魔数校验（PDF/DOCX/DOC） |
| P2-5 | 删除操作不阻塞事件循环 | `knowledge.py` | 同步 os.remove → asyncio.to_thread |
| P2-6 | BM25 延迟批量 rebuild | `bm25.py` | 每次 add/remove 都 O(n) 重建 → 标记 dirty，search 时延迟重建一次 |
| P2-7 | compressor 并发压缩 | `compressor.py` | for 循环逐块串行 await → asyncio.gather + Semaphore(5) 限流 |
| P2-8 | SPA 路径遍历校验 | `app.py` | 直接拼接路径 → resolve() 后断言仍在 FRONTEND_DIST 内 |
| P2-10 | CORS 收紧 | `app.py` | allow_methods/headers=["*"] → 显式列出 GET/POST/PUT/DELETE/OPTIONS + Content-Type/X-API-Key/Authorization/X-Request-ID |
| - | retrieval_logger 异步写 | `retrieval_logger.py` + `qa.py` | 同步 open+write → aiofiles 异步写（log 改 async，qa.py 加 await） |
| - | session 增量更新 | `session.py` | select 全量 + 覆盖写 → SQL JSON_ARRAY_APPEND 增量追加 |
| - | milvus 延迟 flush | `milvus.py` | 每次 insert 都 flush → 标记 dirty，search/count 前 lazy flush |
| - | ingress 盲删优化 | `ingress/service.py` | 盲删 10000 ID → 减小到 2000 ID（4 批×500） |
| - | cache L1 LRU | `cache.py` | 全局字典无上限 → maxsize=1000 + LRU 淘汰 |

验证：`py_compile` 13 个文件全部通过 ✅

未完成（留待后续）：
- P1-4 依赖锁文件
- P2-2 抽取重复逻辑（_prepare_qa_context 等）
- keyword/__init__.py _bm25_indexes LRU（知识库数量通常很少，收益低，跳过）

## P2-2 抽取重复逻辑（18:17）
4 处重复逻辑抽取，9 个文件编译检查全部通过：

| 抽取项 | 文件 | 优化前 → 优化后 |
|---|---|---|
| `shared/json_utils.py`（新建） | `exam_engine.py` + `rerank.py` | 两处内联 JSON 解析（去 code fence + 正则兜底）→ 统一 `parse_llm_json` / `try_parse_llm_json` |
| `paginated_select` | `pagination.py` + 4 个 API 端点 | 4 处重复的 count + select + offset/limit + serialize → 统一 `paginated_select(db, model, page, page_size, where, order_by, serializer)` |
| `_prepare_qa_context` | `qa_engine.py` | `qa_non_stream` / `qa_stream` 大段重复的 guardrail + 检索 + pipeline → 抽取为 `_prepare_qa_context` 返回 (context, sources, block_reason) |
| `_build_sources` 合并 | `qa_engine.py` | `_build_sources(SearchResult)` + `_build_sources_from_chunks(dict)` 两个几乎相同的函数 → 统一为 `_build_sources(items)` 自动识别类型 |
| `_prepare_exam_context` | `exam_engine.py` | `generate_exam` / `generate_exam_stream` 重复的 pipeline 增强 + prompt 构造 → 抽取为 `_prepare_exam_context` |

验证：`py_compile` 9 个文件全部通过 ✅

代码行数变化：
- qa_engine.py: 211 行 → 184 行（-27 行）
- exam_engine.py: 231 行 → 197 行（-34 行）
- rerank.py: 151 行 → 133 行（-18 行）
- 4 个 API 端点：每个减少约 8-10 行重复查询代码
- 新增 json_utils.py: 55 行（复用模块）
- pagination.py: +54 行（paginated_select 函数）
- 净减少约 50 行重复代码，新增 109 行可复用模块
